  <%= simple_form_for [ spot, comment ], html: { data: {spot_comments_target: 'form', action: 'submit->spot-comments#post'}} do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.input :images, as: :file, input_html: {multiple: true} %>
    <%= f.button :button, "Ajouter un commentaire", class: "btn btn-primary btn-submit-form-comment", data: {spot_comments_target: 'btn'} %>


  <% end %>
